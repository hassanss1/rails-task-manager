<%= simple_form_for(@task) do |t| %>
  <%= t.label :title %>
  <%= t.text_field :title %>
  <%= t.label :details %>
  <%= t.text_field :details %>
  <% unless t.object.new_record? %>
    <%= t.check_box :completed %>
    <%= t.label :completed %>
  <% end %>
  <%= t.submit %>
<% end %>
